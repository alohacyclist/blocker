// React Component is a function that returns JSX content

function MyButton() {
  return ( 
    /* HTML & JSX CONTENT */ 
  );
}

// To make the component available we have to export it
export default MyButton;
